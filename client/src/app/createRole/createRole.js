/* global admBoard, angular */

'use strict';
angular.module('gscDatacat.controllers')
        .
        controller('createRoleCtrl', [
            '$scope',
            '$rootScope',
            function(
                    $scope,
                    $rootScope) {

                $scope.dataSource = {};

            }]);
